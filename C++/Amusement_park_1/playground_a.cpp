#include<iostream>
#include "FuncNode.h"
using namespace std;

int main(){
    size=5;
    createArray();
    insertArray(1);
    insertArray(10);
    insertArray(100);
    insertArray(2);
    insertArray(3);
    insertArray(35);
    insertArray(50);
    insertArray(5);
    showAllSum();
    cout<<endl;
    showArray();
    
}